/*
  UPLOAD PAGE

  TODO:
  1. Build file upload UI.
  2. Upload videos to Firebase Storage.
  3. Save metadata (title, tags, timestamp, poster info) to Firestore.
  4. Add upload progress bar.
  5. Allow tag selection based on machine categories.

  Note:
  Navbar already implemented globally.
*/

export default function Upload() {
  return (
    <div className="pt-16 px-4">
      <h1 className="text-xl font-semibold">Upload</h1>
      <p className="text-gray-600 text-sm italic mt-2">
        Upload functionality to be implemented.
      </p>
    </div>
  );
}
