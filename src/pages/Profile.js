/*
  PROFILE PAGE

  TODO:
  1. Display user profile data (name, profile picture, stats).
  2. Add “Edit Profile” functionality.
  3. Show videos posted by the user.
  4. Add logout button (if not placed elsewhere).
  5. Potential extra: link to GymBuddy or Settings.

  Note:
  Navbar already implemented globally.
*/

export default function Profile() {
  return (
    <div className="pt-16 px-4">
      <h1 className="text-xl font-semibold">Profile</h1>
      <p className="text-gray-600 text-sm italic mt-2">
        Profile details and user data to be implemented.
      </p>
    </div>
  );
}
