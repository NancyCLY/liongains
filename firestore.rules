rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows any authenticated user (signed in) to read and write all documents.
    // This is the common "safe test" mode for development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}